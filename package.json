{
  "name": "old-age-home-desktop",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\" \"electron .\"",
    "server": "cd backend && npm start",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "concurrently": "^8.2.0"
  }
}
